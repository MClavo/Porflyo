micronaut:
  test:
    resources:
      enabled: false

# Configuración por defecto para tests - sin DynamoDB ni contenedores
# Los tests unitarios no necesitan infraestructura externa

logger:
  levels:
    com.porflyo: DEBUG
    root: INFO