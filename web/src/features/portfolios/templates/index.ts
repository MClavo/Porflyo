import type { TemplateId } from './types';
//import MvpTemplate from '../../../templates/mvp-01/MvpTemplate';

// Re-export types
export type { TemplateId, TemplateMeta, TemplatesRegistry } from './types';

/**
 * Default template ID
 */
export const DEFAULT_TEMPLATE: TemplateId = 'default';

